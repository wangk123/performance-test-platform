global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: jmx-exporter
    file_sd_configs:
      - files:
          - /etc/prometheus/file_sd/jmx-targets.json
        refresh_interval: 10s
